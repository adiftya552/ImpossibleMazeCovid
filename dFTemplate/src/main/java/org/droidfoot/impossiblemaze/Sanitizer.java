package org.droidfoot.impossiblemaze;
import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Sanitizer here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Sanitizer extends Actor
{
    /**
     * Act - do whatever the Sanitizer wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public Sanitizer()
    {
        setImage("HandSaniz.png");
        getImage().scale(getImage().getWidth() / 12 , getImage().getHeight() / 12);
    }
    public void act() 
    {
        // Add your action code here.
    }    
}
